- name: First playbook
  hosts: [awsvm azurevm]
  become: true
  tasks:
  - name: ping test
    ping:
  - name: update apt cache
    apt: 
     update_cache: yes
  - name: install nginx
    apt: name=nginx state=latest
  - name: start nginx
    service:
      name: nginx
      state: started
